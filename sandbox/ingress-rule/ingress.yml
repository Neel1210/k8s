apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sandbox-ingress
  namespace: sandbox
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /executor-ingester
            pathType: Prefix
            backend:
              service:
                name: executor-ingester-service
                port:
                  number: 8080
          - path: /worker-service
            pathType: Prefix
            backend:
              service:
                name: worker-service
                port:
                  number: 8080
