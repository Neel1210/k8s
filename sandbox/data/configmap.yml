apiVersion: v1
kind: ConfigMap
metadata:
  name: sandbox-config
  namespace: sandbox
data:
  NAME_SPACE: "sandbox"
  RESULT_TIMEOUT: "10" #units in minutes
  app.executor.language-config-file-path: "file:/files/executor/language-config.json"
  