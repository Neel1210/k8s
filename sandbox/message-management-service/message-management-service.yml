apiVersion: apps/v1
kind: Deployment
metadata:
  name: message-management-deployment
  namespace: sandbox
  labels:
    app: message-management
spec:
  replicas: 1
  selector:
    matchLabels:
      app: message-management
  template:
    metadata:
      labels:
        app: message-management
    spec:
      containers:
        - name: message-management
          image: message-management:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
          env:
            # 🔽 add RabbitMQ envs here
            - name: SPRING_RABBITMQ_HOST
              value: "141.148.196.163"  # or better: host's private IP
            - name: SPRING_RABBITMQ_PORT
              value: "5672"
            - name: SPRING_RABBITMQ_USERNAME
              value: "admin"
            - name: SPRING_RABBITMQ_PASSWORD
              value: "admin"
---
apiVersion: v1
kind: Service
metadata:
  name: message-management-service
  namespace: sandbox
spec:
  type: ClusterIP
  selector:
    app: message-management
  ports:
    - name: http
      port: 8081
      targetPort: 8081
