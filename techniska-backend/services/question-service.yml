apiVersion: apps/v1
kind: Deployment
metadata:
  name: question-service-deployment
  namespace: techniska-backend
  labels:
    app: question-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: question-service
  template:
    metadata:
      labels:
        app: question-service
    spec:
      containers:
        - name: question-service
          image: techniska-question-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080

          envFrom:
            - configMapRef:
                name: techniska-config
            - secretRef:
                name: techniska-secret 
                
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"

---
apiVersion: v1
kind: Service
metadata:
  name: question-service
  namespace: techniska-backend
spec:
  type: ClusterIP
  selector:
    app: question-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080